<%= form_for @product, remote: true, authenticity_token: true  do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label 'Nome' %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label 'Preço' %>
    <%= form.text_field :price %>
  </div>
  <div>
    <%= form.submit "Adicionar Produto", class: "btn btn-primary", data: {send: "product"} %>
  </div>
<% end %>
